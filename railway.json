{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build -w backend && npx prisma generate --schema apps/backend/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema apps/backend/prisma/schema.prisma && npm start -w backend",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE"
  }
}
